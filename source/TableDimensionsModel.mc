class TableDimensionsModel {
    var x;
    var y;
    var cellWidth;
    var cellHeight;

    function initialize(x, y, cellWidth, cellHeight){
        self.x = x;
        self.y = y;
        self.cellWidth = cellWidth;
        self.cellHeight = cellHeight;
    }
}